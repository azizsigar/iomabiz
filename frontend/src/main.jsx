import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Update the path to the App.jsx file
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
